/**
 * Clean slate — the new portfolio starts here.
 * Nothing from the old version remains.
 */
export default function App() {
  return (
    <main className="grid min-h-screen place-items-center bg-bg">
      <p className="font-body text-xs uppercase tracking-[0.4em] text-muted">
        Clean slate
      </p>
    </main>
  );
}
